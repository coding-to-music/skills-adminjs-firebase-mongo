const DomainRegistrations = require("./domainRegistration.model");
const Domains = require("./domains.model");
const SkillUser = require("./skills.user.model");

module.exports = {
  DomainRegistrations,
  Domains,
  SkillUser,
};
